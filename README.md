# Background-removal-app
A python app that uses three clustering methods: k-means, dbscan and mean shift to remove the backgrounds of images, created as a way to test which algorithm would be best at it
